require 'spec_helper'

describe AsciiPlist do
  it 'has a version number' do
    expect(AsciiPlist::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
